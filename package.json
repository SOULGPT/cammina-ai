{
  "name": "cammina-ai",
  "version": "1.0.0",
  "private": true,
  "description": "Cammina AI – Monorepo (pnpm workspaces)",
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "lint": "pnpm --filter web lint",
    "format": "pnpm --filter web format"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}
